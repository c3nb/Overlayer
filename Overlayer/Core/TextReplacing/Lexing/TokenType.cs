namespace Overlayer.Core.TextReplacing.Lexing
{
    public enum TokenType
    {
        Identifier,
        TagStart,
        TagEnd,
        Colon,
        ArgStart,
        ArgEnd,
        Comma,
    }
}
